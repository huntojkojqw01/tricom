%ol.breadcrumb
  %li= link_to (t 'title.menu'), main_path
  %li= link_to (t 'title.bashokubunmst'), bashokubunmsts_path
  %li.active= (t 'title.new')
= provide(:title, (t 'title.bashokubunmst'))
= render 'form', mode: :new